# Demo

[FastAPI 0.115.0, mapped](demo/fastapi/index.html) is what this tool writes for a
repository it has never seen before: one tree walk, no model involved. The page
was built by [`docs/demo/build.sh`](demo/build.sh); see
[`docs/demo/fastapi/README.md`](demo/fastapi/README.md) for the tag, the date
and the exact command that produced it.
